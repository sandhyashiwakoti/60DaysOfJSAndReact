function AboutPage() {
  return (
    <div>
      <h2>About This Blog</h2>
      <p>This is a demo blog built with React to consolidate my learning!</p>
    </div>
  );
}

// The key is this line. It makes `AboutPage` the default thing
// this file provides to other files.
export default AboutPage;
